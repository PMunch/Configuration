if [ $# -eq 1 ]
then
	filename=$(basename "$1")
	if [ "${filename##*.}" == "tex" ]
	then
		rm ${filename%%.*}.aux
		rm ${filename%%.*}.log
	fi
else
	echo "Only takes one argument, the .tex file to clean up after"
fi